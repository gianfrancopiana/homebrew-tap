# Gianfranco's Homebrew tap

Install Vivero:

```sh
brew install gianfrancopiana/tap/vivero
```

This tap is updated by the Vivero release workflow. The initial formula builds from the current Vivero source until the first tagged binary release publishes the generated release formula.
